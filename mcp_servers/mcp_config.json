{
  "mcpServers": {
    "workspace-tools": {
      "command": "python",
      "args": ["-m", "mcp_servers.server", "--transport", "stdio"],
      "env": {
        "PYTHONPATH": ".."
      }
    }
  }
}
